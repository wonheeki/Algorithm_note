list = map(int,input().split(" "))

# list_sorted = sorted(list, reverse=True)
# print(list_sorted.pop(), list_sorted.pop(),list_sorted.pop())

list_sorted = sorted(list)
print(list_sorted[0],list_sorted[1],list_sorted[2])

